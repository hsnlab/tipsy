{
  "properties": {
    "type": {
      "type": "string",
      "enum": ["erfs"],
      "description": "erfs: L. Molnar et al., 'Dataplane specialization for high performance OpenFlow software switching,' in ACM SIGCOMM, 2016, pp. 539-552."
    },
    "erfs-dir": {
      "type": "string",
      "default": "/opt/erfs",
      "description": "A directory on SUT in which erfs is installed"
    }
  }
}
